# Sanitized example release

This directory contains only synthetic identifiers and zero-value example
evidence. It contains no live credential, authenticated endpoint, reusable
signed transaction, or claimed experiment result.
